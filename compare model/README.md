# Compare Models

本目录用于存放对比模型运行结果（按模型分开）。

- `CNNLSTM/`
- `iTransformer/`
- `TimeMixer/`

运行建议：优先在 `TimeKAN-main` 下执行 `scripts/Battery/compare_models.sh`。
运行后默认结果位于：`TimeKAN-main/results/<model>/<setting>/`。
如需归档，可将结果复制到本目录对应子文件夹。
